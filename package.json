{
  "name": "otterclaw",
  "version": "0.1.0",
  "private": true,
  "description": "OtterClaw skill runtime — capability-gated execution and SecClaw coordination.",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "node --import tsx --test src/__tests__/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "eslint": "^9.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0"
  }
}
